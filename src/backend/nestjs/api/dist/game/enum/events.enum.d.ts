export declare enum clientEvents {
    connected = "connected",
    update = "update",
    mode = "mode"
}
export declare enum serverEvents {
    updated = "updated",
    ready = "ready",
    started = "started",
    finished = "finished",
    waiting = "waiting",
    playing = "playing"
}
