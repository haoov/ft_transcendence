export enum clientEvents {
	connected = "connected",
	update = "update",
	mode = "mode",
};

export enum serverEvents {
	updated = "updated",
	ready = "ready",
	started = "started",
	finished = "finished",
	waiting = "waiting",
	playing = "playing",
};