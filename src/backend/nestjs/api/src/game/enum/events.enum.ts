export enum ClientEvents {
	connected = "connected",
	up = "up",
	down = "down",
	left = "left",
	right = "right",
};

export enum ServerEvents {
	position = "position",
	finished = "finished",
	disconnect = "disconnect",
	waiting = "waiting",
};